from setuptools import setup

setup(
    name='optool',
    version='1.9.4',
    py_modules=['optool'],
    install_requires=[
        'numpy','matplotlib','math','copy','re','os','subprocess','diskutils','random'
    ]
)
